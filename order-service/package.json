{
  "name": "order-service",
  "version": "1.0.0",
  "description": "Order Service for the distributed e-commerce system",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "cross-env NODE_ENV=test dotenv -e .env.test jest"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "kafka-node": "^5.0.0",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
